method: grid
name: sweep
metric:
  goal: maximize
  name: mean_npep_1pct
parameters:
  model:
    values:
      - baseline
      # - ctree
      - lgbm
      # - rf
      # - xgb
      # - catboost
  grid:
    values:
      - true
      - false
